npm install


npm start



git config --global user.email "your-email@example.com"
git config --global user.name "Your Name"


git add .
git commit -m "Initial commit"
git branch -M main
git push -u origin main


npm run deploy


go to your repo on GitHub → Settings → Pages → set source to branch gh-pages, folder / (root), and click Save.